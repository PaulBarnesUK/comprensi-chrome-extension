/**
 * Video Watcher Module
 * 
 * Detects when a user is watching a YouTube video and tracks watch time and progress.
 * Sends watch events to the background script for storage.
 */

export { initVideoWatchDetector } from './detector';