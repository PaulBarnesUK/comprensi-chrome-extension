/**
 * Video Comparison Module
 *
 * Handles comparing videos for difficulty and collecting user feedback.
 * Shows comparison UI when appropriate based on language and recency.
 */

export { handleVideoEnd } from './core/comparison';
